<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Form</title>
</head>
<body>
    
    <form action="/admin/courses/<%= course._id %>/edit?_method=PUT" method="POST">
        <label for="title" >Title:</label>
        <input type="text" id="title" name="title" value="<%= course.title %>">
        <label for="level">level:</label>
        <select name="level" id="level" value="<%= course.level %>">
            <option value="Advanced">Advanced</option>
            <option value="Intermediate">Intermediate</option>
            <option value="Beginner" selected>Beginner</option>
        </select>
        <label for="duration">duration(in hours):</label>
        <input type="number" id="duration" name="duration" value="<%= course.duration %>">
        <button type="submit">Submit</button>
    </form>

</body>
</html>